---
name: keke-kemei-drama
description: 为固定猫耳双主角「可可」（银白短发少年，活泼搞笑）与「可妹」（银白长卷发少女，傲娇可爱）生成二次元日常竖屏短剧。用户用自然语言讲大概剧情（可以粗糙、缺细节），本技能先询问用户期望总时长，再把剧情优化成逻辑通顺的表格形式分段台词本（时间码/画面动作/台词三列，动作写到每只手），经用户确认后按每段 10 秒分段生成、段间流畅衔接的 9:16 视频。当用户提到"给可可可妹做个短剧/视频""可可可妹动画""可可可妹小剧场"，或"我有个剧情大概你帮我做出来"等基于这两个固定猫耳角色做日常搞笑/情感小剧场的表达时使用。台词、动作、衔接问题一律先优化脚本、确认后再生视频，禁止直接出片。
---

# 可可与可妹 · 10 秒分段短剧

## 定位

固定猫耳双主角的二次元日常短剧生产线。**用户只负责讲一个大概剧情（可以很糙、缺细节）**，本技能负责把它优化成可直接生成的分段脚本，确认后逐段生成视频。

- **角色**：可可（男，活泼搞笑）、可妹（女，傲娇可爱）；配角：小咪（黑猫）、小旺（小狗）。外观锚点见 `references/characters.md`，**发色/猫耳/配件永远不变，服装可随场景换**。小咪小旺默认不说话，靠动作表情卖萌。
- **画风**：二次元细腻插画风、柔和光照、温馨日常感，首句风格锚点一字不差。

## 核心流程

```text
用户讲大概剧情 → 询问总时长预期 → 优化成分段台词本(表格) → 用户确认 → 逐段生成 → 交付
```

1. **接故事 + 问时长（强制）**：用户用自然语言讲剧情后，**必须先询问用户期望的总时长**（例如："你希望这个小剧场大概多长？30秒？1分钟？2分钟？"），再根据总时长决定分几段（每段固定10秒，总时长÷10=段数，向上取整）。用户没说时长就默认问一句，不要自己拍板。缺表情、动作、衔接细节时**不打断追问，内部按人设补全**；只有剧情逻辑本身有硬伤（人物动机矛盾）才追问。
2. **优化脚本（强制）**：按 `references/storyboard.md` 把粗糙剧情改写成：一句话梗概；按用户确认的总时长 ÷ 10秒拆分段落；**输出为表格形式**（列：时间码 / 画面动作 / 台词），其中"画面动作"列必须写得非常详细——每个角色每只手在做什么、手指姿势、道具位置、身体朝向、表情变化，都要写死，防止生成视频时出现多手、换衣服、手指数量错误；每段**前 3 秒有钩子**、结尾有反转或悬念；逻辑改顺，对话用表格台词列，禁止"来回说三次"式自然语言。**输出台词本之前必须先按 storyboard.md 的"输出前自检"清单逐项检查**（道具位置、动作因果、谁在哪、笑点落地、手部数量、服装一致性），发现硬伤先改好再输出。
3. **确认门禁（强制）**：输出完整分段台词本（表格），等用户明确说"确认/可以/开始"后才生成视频。
4. **生成视频**：每段 **10 秒、竖屏 9:16**，以 `assets/` 人设图做人设参考（I2V）；台词用 `{台词}` 包裹；**默认不加字幕**，仅当用户明确要求字幕时才在画面底部加；**单批最多 2 段**。
5. **交付**：生成一段、展示一段，不囤积。

## 衔接规则（强制，针对"两段场景跳了/接不上"）

- **同一段剧情里，相邻两段必须同一场景**：第 1 段在客厅，第 2 段不能无故变大街。场景只在用户明确写了「转场：从 X 到 Y」时才允许换。
- **段间衔接**：上一段结尾的画面/动作/语气 = 下一段开头的延续（动作接上、机位不跳、光线连续）。
- **光线与亮度**：每段脚本必须写清光线四要素（时段/主光方向/色温/整体亮度）；同场景多段之间逐字一致。第 2 段起必须用上一段最后一帧做首帧参考，并在 prompt 里写"与上一段相同的曝光、亮度、色温"，防止拼接时亮度跳变。

## 角色一致性（强制）

- 生成视频时必须以 `assets/` 人设图做人设参考（I2V），禁止纯文生导致换脸。
- 每个镜头 prompt 复述外观锚点（银白发色、猫耳猫尾、可妹黑猫发夹+蓝发卡、体型比例）。
- **体型/年龄跨段锁定（强制）**：每段都必须复述"Q版、圆脸大头、同一年龄感"，即使转场新场景也不允许一段像小学生、一段像高中生；并传入 `assets/可可可妹_角色设定.jpg` 锁头身比（约2.5头身、头大身短，**可可比可妹略高一点**），防止人变高变长。
- **服装随场景换，但同一场景/同一批镜头里服装必须一致**：本场服装一旦定好，每段 prompt 都逐字复述，并写明"参考图只锁五官发色猫耳，服装按本段描述，不换回人设图衣服"。
- **服装图案统一**：衣服上的印花/图案必须和猫耳主题一致（猫爪印、小猫图案），画风跟随人设图，不出现无关印花。
- **声线全片锁定（强制）**：以"餐厅算账"视频的声线为标准——可可=清亮少年音语速快带调皮、可妹=清甜少女音娇俏尾音上扬。**只要角色出声（说话/惊叫/叹气/打呼），每段 prompt 都必须复述对应声线，包括只有拟声的段落**，不允许两段声音变脸或换年龄感。

## 动作规则（针对"多手/换衣服/手势画错"）

- **每只手都要写死**：脚本表格的"画面动作"列里，每个角色的每只手都必须单独写明——左手在做什么、右手在做什么、手指姿势、握着什么道具。不允许只写"可可在吃零食"这种模糊描述，必须写"可可右手抓一把薯片送进嘴里，左手搭在沙发扶手上"。
- 手指姿势标准写法：
  - 石头 = 五指握拳；
  - 剪刀 = 伸出食指和中指两根手指，其余三根握拳；
  - 布 = 五指张开；
  - 拿东西 = 写明哪只手、握什么、手指自然弯曲握住。
- **禁止多手/多肢体**：脚本里每个角色只描述两只手、两条腿，prompt 里必须加负向约束"每个角色只有两只手、两条手臂，不出现第三只手或多余手臂"。
- **服装锁定（防突然换装）**：本场服装在脚本开头一次性写死完整描述（上衣+裤子/裙子+袜子+鞋子+图案），每段表格的动作列里如果角色身体有移动，都要顺带确认"服装不变"；生成视频时每段 prompt 逐字复述完整服装，并加负向约束"服装全程不变，不突然换衣服、不换回人设图默认服装"。
- **禁止手指数量错误**（如"剪刀"只伸一根食指）。

## 视频参数

- 每段 **10 秒**；竖屏 **9:16**；二次元细腻插画风、柔光。
- 台词配音随视频生成；**默认不加字幕**，仅当用户明确要求时才加与台词一致的字幕。

## 质量门（生成前自查）

- [ ] 已询问用户总时长预期，并据此确定段数。
- [ ] 分段台词本为表格形式（时间码/画面动作/台词三列），已获用户确认。
- [ ] 相邻段场景一致（或有明确转场标注）。
- [ ] 段尾衔接画面 = 下一段开头延续。
- [ ] 每段前 3 秒有钩子，结尾有反转/悬念。
- [ ] 画面动作列里每个角色的每只手都写清了姿势和道具，无模糊描述。
- [ ] 服装在脚本开头写死完整描述，每段动作列确认服装不变。
- [ ] 角色锚点复述完整，服装同场景内一致。
- [ ] 单批 ≤ 2 段。
- [ ] 用户未要求字幕时，prompt 里没有"加字幕"指令。
- [ ] 相邻段亮度/色温目视一致，拼接无明暗跳变。
- [ ] 相邻段服装一致，没换回人设图默认装；声线一致。
- [ ] prompt 里含负向约束：不多手、不多手臂、不多尾巴、不多耳朵、服装不变。

## 交付前帧检（强制，先查后交）

每段视频生成后，**先下载、抽中尾帧自己检查，通过了再交给用户**，不把有问题的片子直接发出去：

1. 下载该段视频，用 ffmpeg 抽 1-2 帧（中间帧 + 结尾帧）并 Read 看图；
2. 逐项核对：
   - 人脸/年龄感/体型与上一段、与人设图一致，不是另一个年龄的人；**可妹比可可矮半个头，身高差和上一段一致**，没有谁突然变高/变矮；
   - 出场角色数量正确——小咪只有一只、小旺只有一只，没有多画/重复；**小咪小旺都是正常四条腿，没有六条腿之类的畸变**；
   - **每个角色只有一条尾巴、两只耳朵**，不多尾巴、不多耳朵；
   - **每个角色只有两只手、两条手臂**，无第三只手、无多余手臂、无手指数目错误；
   - 服装、图案与脚本完全一致，**没有突然换衣服、没有换回默认装**；
   - **和上一段尾帧并排看：亮度、色温、构图肉眼无跳变**，不像两段拼的。
3. 发现问题：**当场重生成该段**（在 prompt 里补上出错点的明确描述），重抽帧复检；连续失败才告知用户；
4. 全部通过后才 present_files 交付。

## 路由

| 内容 | 文件 |
|---|---|
| 角色外观锚点 | `references/characters.md` |
| 脚本优化规则与分段模板 | `references/storyboard.md` |
| 视频 prompt 写法 | `references/prompt-templates.md` |
| 平台适配 | `references/platforms.md` |
